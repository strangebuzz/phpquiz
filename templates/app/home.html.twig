{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="preconnect" href="{{ preconnect('https://platform.twitter.com') }}" />
{% endblock %}

{% block body %}
    <div class="header">
        <h1>PHPQuiz 🐘</h1>
    </div>

    <div class="content">
        <p>
            There is currently {{ count }} questions in the quiz. Click on the links
            below to start. Go go go! 😀
        </p>

        <ul>
            <li><a href="{{ path('question_show', {id: 1}) }}">Pass all quiz sequentially</a></li>
            <li><a href="{{ path('question_random') }}">Show a random quiz</a></li>
        </ul>

        <p>For now, there arent' a lot of quiz, so you can </p>
    </div>
{% endblock %}
